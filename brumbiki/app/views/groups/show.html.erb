<h1><%= @group.name %> - Wiki Page</h1>
<form action="/groups/<%= @group.id %>/memberships" method="post" >
  <input type="submit" value="Join This Group" class= "btn btn-default btn-primary col-md-4" >
</form>
<br>
<h3><%= link_to "Add an article", new_group_article_path(@group) %> </h3>
<h3>Articles:</h3>
<ul>
<% @group.articles.each do |article| %>
   <li><%= link_to article.versions.last.title, article_versions_path(article) %>
    (updated <%= article.versions.last.created_at.strftime("%m/%d") %>)</li>
<% end %>
</ul>
